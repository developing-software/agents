# sandboxd-sdk

Typed client for the [sandboxd](https://github.com/developing-software/sandboxd) control plane,
generated by hey-api from its `api/client.yaml`.

```bash
bun run gen   # regenerate src/ from the upstream contract
```

Service-token gated: use it on a server only. `@agents/core/sandbox` wraps it.
